---
import type { Lang } from '../../i18n/ui';
import { useTranslations } from '../../i18n/utils';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
---

<section class="mt-12 p-6 md:p-8 bg-night-800/30 rounded-2xl border border-night-700/30">
  <h2 class="font-title text-2xl font-bold text-gold-400 mb-4">
    {t('books.materials')}
  </h2>
  <p class="text-night-300">
    {t('books.materialsDescription')}
  </p>
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mt-6">
    <div class="p-4 bg-night-800/50 rounded-xl text-center border border-night-700/20">
      <div class="text-3xl mb-2">🎥</div>
      <p class="text-night-400 text-sm font-semibold">{lang === 'es' ? 'Videos' : 'Videos'}</p>
    </div>
    <div class="p-4 bg-night-800/50 rounded-xl text-center border border-night-700/20">
      <div class="text-3xl mb-2">🎙️</div>
      <p class="text-night-400 text-sm font-semibold">{lang === 'es' ? 'Podcast' : 'Podcast'}</p>
    </div>
    <div class="p-4 bg-night-800/50 rounded-xl text-center border border-night-700/20">
      <div class="text-3xl mb-2">📄</div>
      <p class="text-night-400 text-sm font-semibold">{lang === 'es' ? 'Guías' : 'Guides'}</p>
    </div>
  </div>
</section>
